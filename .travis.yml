language: ruby
branches:
  only:
    - master
rvm:
  - 2.7.1
cache: bundler
before_install:
  - gem install bundler
before_script:
  - sudo apt-get -y install pngquant
  - sudo gem install git
  - gem install nokogiri
  - chmod +x ./deploy.sh
script: ./deploy.sh
